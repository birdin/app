import { FiSettings } from "react-icons/fi"

const SettingsPlaceholder = () => {
  return (
    <div className={`project-form-placeholder__avatar`}>
      <FiSettings />
    </div>  )
}

export default SettingsPlaceholder